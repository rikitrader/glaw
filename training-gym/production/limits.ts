export interface ResourceQuota { maxSteps: number; maxToolCalls: number; maxDurationMs: number; maxModelTokens: number; maxArtifactBytes: number; }
export interface ResourceUsage { steps: number; toolCalls: number; durationMs: number; modelTokens: number; artifactBytes: number; }
export function enforceQuota(usage: ResourceUsage, quota: ResourceQuota): void { const pairs: Array<[keyof ResourceUsage, keyof ResourceQuota]> = [['steps','maxSteps'],['toolCalls','maxToolCalls'],['durationMs','maxDurationMs'],['modelTokens','maxModelTokens'],['artifactBytes','maxArtifactBytes']]; for (const [used, max] of pairs) if (usage[used] > quota[max]) throw new Error(`Resource quota exceeded: ${String(used)}`); }
export class ConcurrencyLimiter { private active = 0; private readonly limit: number; constructor(limit: number) { this.limit = limit; if (limit < 1) throw new Error('Concurrency limit must be positive'); } tryAcquire(): boolean { if (this.active >= this.limit) return false; this.active += 1; return true; } release(): void { if (this.active > 0) this.active -= 1; } get inUse(): number { return this.active; } }
