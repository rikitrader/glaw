import test from 'node:test'; import assert from 'node:assert/strict'; import { InsuranceClaimsGym } from '../gyms/insurance-claims/index.ts';
test('insurance claims gym requires source-grounded findings for every line',async()=>{const gym=new InsuranceClaimsGym();await gym.reset({seed:42});for(const [line_id,classification] of [['L-001','SUPPORTED'],['L-002','PARTIALLY_SUPPORTED'],['L-003','UNSUPPORTED']] as const)await gym.step({tool:'insurance_claims.add_finding',arguments:{line_id,classification,rationale:'Supported by the supplied claim record and evidence.'}});await gym.step({tool:'insurance_claims.complete_review',arguments:{source_verified:true}});assert.equal((await gym.evaluate()).success,true);});
test('insurance claims gym rejects unknown findings',async()=>{const gym=new InsuranceClaimsGym();await gym.reset();await assert.rejects(gym.step({tool:'insurance_claims.add_finding',arguments:{line_id:'L-999',classification:'SUPPORTED',rationale:'guess'}}),/unknown claim line/);});
