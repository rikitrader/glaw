export interface DeadlineRule { ruleId:string; jurisdiction:string; issue:string; trigger:string; days:number; calendarDays:boolean; validityFrom:string; validityTo:string|null; authorityRefs:string[]; exceptions:string[]; }
export interface DeadlineResult { deadline:string|null; calculation:string; trigger:string; authorityRefs:string[]; exceptions:string[]; confidence:number; status:'CALCULATED'|'MISSING_TRIGGER'|'RULE_UNRESOLVED'; }
function addDays(iso:string,days:number):string { const date=new Date(`${iso}T00:00:00Z`); date.setUTCDate(date.getUTCDate()+days); return date.toISOString().slice(0,10); }
export function calculateDeadline(rule:DeadlineRule|undefined, triggerDate:string|undefined):DeadlineResult { if(!rule) return {deadline:null,calculation:'NO_VERIFIED_RULE',trigger:'UNKNOWN',authorityRefs:[],exceptions:[],confidence:0,status:'RULE_UNRESOLVED'}; if(!triggerDate) return {deadline:null,calculation:'TRIGGER_DATE_REQUIRED',trigger:rule.trigger,authorityRefs:rule.authorityRefs,exceptions:rule.exceptions,confidence:0,status:'MISSING_TRIGGER'}; return {deadline:addDays(triggerDate,rule.days),calculation:`${triggerDate} + ${rule.days} ${rule.calendarDays?'calendar':'business'} days`,trigger:rule.trigger,authorityRefs:rule.authorityRefs,exceptions:rule.exceptions,confidence:rule.authorityRefs.length?80:0,status:rule.authorityRefs.length?'CALCULATED':'RULE_UNRESOLVED'}; }
