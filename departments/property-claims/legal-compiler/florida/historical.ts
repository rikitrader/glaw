import { createHash } from 'node:crypto';

export interface FloridaStatutoryVersion { authorityId:string; citation:string; versionId:string; validFrom:string; validTo:string|null; sourceSnapshotId:string; sourceUrl:string; operativeText:string; amendedBy:string[]; supersededBy:string[]; applicabilityNotes:string[]; effectiveDateVerified:boolean; applicabilityVerified:boolean; verified:boolean; }
export interface LegalTemporalContext { policyInception:string|null; policyRenewal:string|null; endorsementEffectiveDate:string|null; dateOfLoss:string; claimNotice:string|null; supplementDate:string|null; appraisalDemand:string|null; preSuitNotice:string|null; suitFiling:string|null; judgment:string|null; }

export function selectFloridaStatutoryVersion(versions:FloridaStatutoryVersion[], context:LegalTemporalContext):FloridaStatutoryVersion|null { return versions.filter((version)=>version.verified&&version.validFrom<=context.dateOfLoss&&(version.validTo===null||context.dateOfLoss<version.validTo)).sort((a,b)=>b.validFrom.localeCompare(a.validFrom))[0]??null; }
export function verifyHistoricalVersion(version:FloridaStatutoryVersion):string[] { const errors:string[]=[]; if(!version.sourceSnapshotId) errors.push('source snapshot missing'); if(!version.sourceUrl.startsWith('https://')) errors.push('source URL is not secure'); if(!version.operativeText) errors.push('operative text missing'); if(!version.effectiveDateVerified) errors.push('effective date not verified'); if(!version.applicabilityVerified) errors.push('applicability not verified'); if(version.verified&&errors.length) errors.push('verified flag conflicts with missing historical requirements'); return errors; }
export function statutoryTextHash(text:string):string{return createHash('sha256').update(text,'utf8').digest('hex');}
