import type { FloridaCaseOpinion } from './opinions.ts';
export interface DCAConflict { conflictId:string; issue:string; cases:string[]; districts:string[]; holdings:string[]; policyDifferences:string[]; supremeCourtResolution:string|null; statutoryResolution:string|null; forumEffect:string; status:'NO_CONFLICT'|'POTENTIAL_CONFLICT'|'RESOLVED_BY_SUPREME_COURT'|'RESOLVED_BY_STATUTE'|'UNRESOLVED'; humanReviewRequired:boolean; }
export function analyzeDCAConflict(opinions:FloridaCaseOpinion[], issue:string):DCAConflict { const relevant=opinions.filter((opinion)=>opinion.issueCodes.includes(issue)&&opinion.court.endsWith('DCA')); const holdings=[...new Set(relevant.map((opinion)=>opinion.holding.trim()).filter(Boolean))]; const districts=[...new Set(relevant.map((opinion)=>opinion.district??opinion.court))]; const conflict=holdings.length>1; return { conflictId:`FL-DCA-${issue}`, issue, cases:relevant.map((opinion)=>opinion.caseId), districts, holdings, policyDifferences:relevant.flatMap((opinion)=>opinion.policyDependency), supremeCourtResolution:null, statutoryResolution:null, forumEffect:conflict?'controlling district and forum must be evaluated':'no conflicting DCA holding represented in supplied record', status:conflict?'POTENTIAL_CONFLICT':'NO_CONFLICT', humanReviewRequired:conflict }; }
