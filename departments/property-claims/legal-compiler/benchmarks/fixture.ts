import type { LegalIssueCode } from '../types/index.ts';
export interface LegalBenchmarkFixture { fixtureId:string; jurisdiction:string; lossDate:string; issue:LegalIssueCode; policy:Record<string,unknown>; facts:Record<string,unknown>; expectedAuthorities:string[]; expectedClassification:string; prohibitedAuthorities:string[]; humanReviewExpected:boolean; }
export function validateFixture(fixture:LegalBenchmarkFixture):string[] { const errors:string[]=[]; if(!fixture.fixtureId||!fixture.jurisdiction||!fixture.lossDate||!fixture.issue) errors.push('fixture identity, jurisdiction, date, and issue are required'); if(!fixture.expectedAuthorities.length) errors.push('fixture must declare expected authorities'); if(fixture.prohibitedAuthorities.some((id)=>fixture.expectedAuthorities.includes(id))) errors.push('prohibited authority cannot also be expected'); return errors; }
