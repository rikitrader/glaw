import type { PolicyProvision } from '../../../src/domain.ts';
export type PolicyDoctrine='EXCLUSION'|'COVERAGE_GRANT'|'ANTI_CONCURRENT_CAUSATION'|'ENSUING_LOSS'|'BURDEN_OF_PROOF'|'CONDITION'|'VALUATION'|'APPRAISAL'|'LIMITATION'|'UNKNOWN';
export interface PolicyLawMapping { provisionId:string; doctrine:PolicyDoctrine; rationale:string; policyText:string; sourceRef:string; confidence:number; status:'MAPPED'|'REQUIRES_CONTEXT'; }
export function mapProvisionToDoctrine(provision:PolicyProvision):PolicyLawMapping { const text=provision.text.toLowerCase(); let doctrine:PolicyDoctrine='UNKNOWN'; if(provision.kind==='EXCLUSION'||text.includes('do not insure')) doctrine='EXCLUSION'; else if(provision.kind==='GRANT') doctrine='COVERAGE_GRANT'; else if(provision.kind==='APPRAISAL') doctrine='APPRAISAL'; else if(provision.kind==='CONDITION') doctrine='CONDITION'; else if(provision.kind==='LIMIT'||provision.kind==='SUBLIMIT') doctrine='LIMITATION'; const confidence=doctrine==='UNKNOWN'?0:provision.text.length>40?75:45; return {provisionId:provision.provisionId,doctrine,rationale:doctrine==='UNKNOWN'?'context and form identity required':'classification uses provision kind plus policy context',policyText:provision.text,sourceRef:`${provision.docId} p.${provision.page}`,confidence,status:doctrine==='UNKNOWN'?'REQUIRES_CONTEXT':'MAPPED'}; }
