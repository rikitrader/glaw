export interface CompletenessVector { source:number; issue:number; temporal:number; precedent:number; citation:number; tests:number; overall:number; status:'COMPLETE_VERIFIED'|'COMPLETE_WITH_CONFLICT'|'PARTIAL'|'RESEARCH_REQUIRED'|'UNVERIFIED'|'NOT_IMPLEMENTED'; }
export function completenessScore(input:Omit<CompletenessVector,'overall'|'status'>):CompletenessVector { const overall=Math.round((input.source+input.issue+input.temporal+input.precedent+input.citation+input.tests)/6); const status=overall>=95?'COMPLETE_VERIFIED':overall>=75?'COMPLETE_WITH_CONFLICT':overall>=40?'PARTIAL':overall>0?'RESEARCH_REQUIRED':'NOT_IMPLEMENTED'; return {...input,overall,status}; }
