import type { AuthorityNode } from './types.ts';

export interface JurisdictionResolution { state:string; county?:string; federalCircuit?:string; basis:string[]; confidence:number; status:'RESOLVED'|'AMBIGUOUS'|'UNKNOWN'; }
export function resolveJurisdiction(input:{propertyState?:string; propertyCounty?:string; policyState?:string; forum?:string}):JurisdictionResolution { const state=input.propertyState ?? input.policyState; if(!state) return {state:'UNKNOWN',basis:[],confidence:0,status:'UNKNOWN'}; const basis=[input.propertyState?'property location':'policy state']; if(input.propertyState && input.policyState && input.propertyState !== input.policyState) return {state:input.propertyState,county:input.propertyCounty,basis:[...basis,'policy/property state conflict'],confidence:35,status:'AMBIGUOUS'}; return {state,county:input.propertyCounty,basis,confidence:input.forum?90:70,status:input.forum?'RESOLVED':'AMBIGUOUS'}; }
export function validateAuthorityForJurisdiction(node:AuthorityNode, jurisdiction:JurisdictionResolution):string[] { const errors:string[]=[]; if(!node.verified) errors.push('authority is unverified'); if(jurisdiction.status !== 'RESOLVED') errors.push('governing jurisdiction is not resolved'); if(node.jurisdiction !== jurisdiction.state && node.precedentialStatus === 'binding') errors.push('binding authority is from a different jurisdiction'); if(!node.source) errors.push('authority source is missing'); return errors; }
