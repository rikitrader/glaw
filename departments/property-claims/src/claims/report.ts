import type { ClaimDigitalTwin } from '../domain.ts';
import type { AppellateResult, WhiteCourtFinding } from './adjudication/court.ts';
export interface DefensibleReport { title:string; status:'DRAFT'|'HUMAN_REVIEW_REQUIRED'|'READY_FOR_REVIEW'; sections:Record<string,unknown>; unresolved:string[]; humanReviewRequired:boolean; generatedAt:string; }
export function assembleReport(twin:ClaimDigitalTwin, findings:WhiteCourtFinding[], appellate:AppellateResult):DefensibleReport { const unresolved:string[]=[]; if(!twin.policy) unresolved.push('complete policy and endorsements'); if(!twin.claim.jurisdiction) unresolved.push('governing jurisdiction'); if(appellate.outcome.startsWith('REMAND')) unresolved.push(...appellate.reasons); return {title:`GLAW Property Claims Report — ${twin.claim.claimId}`,status:unresolved.length?'HUMAN_REVIEW_REQUIRED':'READY_FOR_REVIEW',sections:{claimIdentity:twin.claim,policy:twin.policy ?? 'NOT_PROVIDED',coverageFindings:findings,estimates:twin.estimates,invoices:twin.invoices},unresolved,humanReviewRequired:true,generatedAt:new Date().toISOString()}; }
