# Core agent manifests

The canonical required-field contract is `../manifest.yaml`; the role registry is `../agent-registry.yaml`. Core agents must never invent sources, quotations, pages, statistics, or outcomes.
