# GLAW Python toolbelt — optional. The core firm (skills + matter state) runs
# with zero dependencies; these power the document/citation/redline tools.
#
#   python3 -m venv ~/.glaw/tools/glaw-venv
#   ~/.glaw/tools/glaw-venv/bin/pip install -r requirements.txt

# citations & court data
eyecite>=2.6
juriscraper>=2.6

# documents & templates
python-docx>=1.1
docxtpl>=0.18
jsonschema>=4.0
requests>=2.31

# real Word tracked-changes + redline PDFs (pulls python-docx, fpdf2, lxml)
legal-redline-tools @ git+https://github.com/evolsb/legal-redline-tools.git

# System dependencies (install separately, not via pip):
#   pandoc, weasyprint         -> PDF / Slides publishing (glaw-publish)
#   tesseract, poppler         -> OCR + PDF rasterization (glaw-doc-extract)
#   Java 21 + Apache Tika jar  -> rich document extraction
#   opendataloader-pdf         -> PDF -> Markdown
