# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.coverage
.pytest_cache/
.nested-git-backups-*/
glaw.backup.*/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# secrets — never commit
.env
*.env
*token*.json
*credentials*.json
.gcp/

# GLAW runtime state (lives in ~/.glaw, never in the repo)
matters/
*.local.md

# vendored bookkeeping engine — commit source, not the venv
lib/bookkeeping/.venv/
lib/bookkeeping/**/__pycache__/
