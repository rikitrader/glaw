#!/usr/bin/env bash
# glaw-fl-cause — query the Florida Causes of Action index (110 causes + legal standards).
set -euo pipefail
GLAW="${GLAW_SKILL_DIR:-$(cd "$(dirname "$0")/.." && pwd)}"
exec python3 "$GLAW/fl-quantum-meruit/lib/fl_causes.py" "$@"
