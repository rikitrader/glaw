#!/usr/bin/env bash
# glaw-fed-cause — query the federal causes of action index (civil/criminal-referral/regulatory).
set -euo pipefail
GLAW="${GLAW_SKILL_DIR:-$(cd "$(dirname "$0")/.." && pwd)}"
exec python3 "$GLAW/lib/federal-litigation/fed_causes.py" "$@"
