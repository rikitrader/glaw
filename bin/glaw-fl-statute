#!/usr/bin/env bash
# glaw-fl-statute — query the Florida Statutes Title VI (Civil Practice & Procedure) index DB.
set -euo pipefail
GLAW="${GLAW_SKILL_DIR:-$(cd "$(dirname "$0")/.." && pwd)}"
exec python3 "$GLAW/fl-quantum-meruit/lib/fl_title6.py" "$@"
