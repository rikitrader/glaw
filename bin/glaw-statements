#!/usr/bin/env bash
# glaw-statements — disabled in absolute zero-third-party-package mode.
set -euo pipefail
echo "ERROR: glaw-statements requires the disabled bookkeeping parser stack." >&2
echo "Use existing stdlib ledger outputs or manually prepared JSON statements." >&2
exit 2
