#!/usr/bin/env bash
# glaw-federal-format — federal-filing render profile in zero-dependency mode.
set -euo pipefail
GLAW="${GLAW:-$HOME/.claude/skills/glaw}"
PY="$(command -v python3)"
exec "$PY" "$GLAW/lib/render/federal_docx.py" "$@"
