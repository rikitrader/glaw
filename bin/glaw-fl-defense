#!/usr/bin/env bash
# glaw-fl-defense — query the Florida affirmative-defenses index.
set -euo pipefail
GLAW="${GLAW_SKILL_DIR:-$(cd "$(dirname "$0")/.." && pwd)}"
exec python3 "$GLAW/fl-quantum-meruit/lib/fl_defenses.py" "$@"
