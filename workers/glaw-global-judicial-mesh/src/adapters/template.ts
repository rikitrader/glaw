import { HttpLegalSourceAdapter } from './base'; import type { CourtJurisdictionCoverage, LegalSearchQuery, ProviderCapabilities, ProviderReview, ProviderSearchResult, SearchContext } from '../types';
export class GenericLegalSourceAdapter extends HttpLegalSourceAdapter { constructor(public readonly id: string, public readonly jurisdiction: string[], public readonly endpoint: string, public readonly coverage: CourtJurisdictionCoverage[] = []) { super(); } review: ProviderReview = { lifecycle: 'DISCOVERED', termsReviewed: false, schemaValidated: false, authorityValidated: false, securityReviewed: false, notes: ['Inactive placeholder. Complete all four reviews and explicitly activate before routing.'] }; capabilities: ProviderCapabilities = { search: false, caseLookup: false, docketLookup: false, citationLookup: false, documentDownload: false, bulkAccess: false, apiAvailable: false, scrapingPermitted: false, authRequired: true, paidAccess: false, robotsRestricted: true }; async searchCases(query: LegalSearchQuery, context: SearchContext): Promise<ProviderSearchResult[]> { this.assertRoutable(); void context; void query; return []; } }
