import type { Env, LegalSourceAdapter, ProviderReview } from './types'; import { CourtListenerAdapter } from './adapters/courtlistener'; import { RecapAdapter } from './adapters/recap'; import { GenericLegalSourceAdapter } from './adapters/template'; import { AsianLIIAdapter, CommonLIIAdapter, HKLIIAdapter, JuriscraperAdapter, KenyaLawAdapter, PacLIIAdapter, WorldLIIAdapter } from './adapters/priority'; import { LocalMockLegalSourceAdapter } from './adapters/local-mock'; import { MOCK_RECORDS } from './mocks/catalog'; import { inactiveReview } from './repositories/provider-reviews';
export function createRegistry(env: Env, reviews: Map<string, ProviderReview> = new Map()): Map<string, LegalSourceAdapter> { const entries: Array<[string, LegalSourceAdapter]> = [['courtlistener', new CourtListenerAdapter(env)], ['recap', new RecapAdapter(env)], ['juriscraper', new JuriscraperAdapter()], ['worldlii', new WorldLIIAdapter()], ['commonlii', new CommonLIIAdapter()], ['asianlii', new AsianLIIAdapter()], ['paclii', new PacLIIAdapter()], ['hklii', new HKLIIAdapter()], ['kenyalaw', new KenyaLawAdapter()], ['canlii', new GenericLegalSourceAdapter('canlii', ['CA'], 'https://www.canlii.org')], ['eurlex', new GenericLegalSourceAdapter('eurlex', ['EU'], 'https://eur-lex.europa.eu')], ['curia', new GenericLegalSourceAdapter('curia', ['EU-CJEU'], 'https://curia.europa.eu')], ['hudoc', new GenericLegalSourceAdapter('hudoc', ['ECHR'], 'https://hudoc.echr.coe.int')], ['icj', new GenericLegalSourceAdapter('icj', ['INTL-ICJ'], 'https://www.icj-cij.org')], ['icc', new GenericLegalSourceAdapter('icc', ['INTL-ICC'], 'https://www.icc-cpi.int')], ['icsid', new GenericLegalSourceAdapter('icsid', ['INTL-ICSID'], 'https://icsid.worldbank.org')], ['pca', new GenericLegalSourceAdapter('pca', ['INTL-PCA'], 'https://pca-cpa.org')], ['itlos', new GenericLegalSourceAdapter('itlos', ['INTL-ITLOS'], 'https://www.itlos.org')], ['bailii', new GenericLegalSourceAdapter('bailii', ['GB','IE'], 'https://www.bailii.org')], ['austlii', new GenericLegalSourceAdapter('austlii', ['AU'], 'https://www.austlii.edu.au')], ['nzlii', new GenericLegalSourceAdapter('nzlii', ['NZ'], 'https://www.nzlii.org')], ['saflii', new GenericLegalSourceAdapter('saflii', ['ZA'], 'https://www.saflii.org')]]; const mockMode = env.ENVIRONMENT === 'local' && env.MOCK_PROVIDER_MODE === 'true'; let registry = new Map(entries); if (mockMode) registry = new Map([...registry.values()].map((provider) => [provider.id, new LocalMockLegalSourceAdapter(provider.id, provider.jurisdiction, provider.endpoint, provider.coverage, MOCK_RECORDS[provider.id] ?? { id: `missing-${provider.id}`, sourceUrl: `mock://${provider.id}/missing`, snippet: 'Synthetic missing fixture.' })] as const)); for (const provider of registry.values()) provider.review = mockMode ? provider.review : (reviews.get(provider.id) ?? inactiveReview()); return registry; }
