import type { GLAWCase, LegalSearchQuery, SourceAuthorityScore } from '../types';
export function scoreCase(item: GLAWCase, query: LegalSearchQuery): SourceAuthorityScore { const officialSource = item.source.official ? 100 : 70; const jurisdictionMatch = query.jurisdiction?.some((j) => item.jurisdiction.country === j || item.court.id === j) ? 100 : 50; const completeness = [item.court.name, item.dates.decided, item.identifiers.docketNumber ?? item.identifiers.neutralCitation].filter(Boolean).length / 3 * 100; const overall = Math.round(officialSource * .2 + jurisdictionMatch * .2 + 60 * .15 + completeness * .15 + 60 * .1 + 70 * .1 + 80 * .05 + (item.authority.confidence ?? 0.5) * 100 * .05); return { officialSource, jurisdictionMatch, courtLevel: 60, completeness, recency: 60, citationReliability: 70, documentIntegrity: 80, subsequentHistoryConfidence: 50, overall }; }
